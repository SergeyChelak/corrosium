[package]
name = "bootloader"
version = "0.1.0"
edition = "2024"

[dependencies]
bootinfo = { path = "../bootinfo" }
log = "0.4.30"
uefi = { version = "0.37.0", features = ["logger", "panic_handler"] }
